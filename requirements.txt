# strm2stl runtime dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi==0.135.1
uvicorn==0.41.0
pydantic==2.12.5
orjson>=3.9
starlette==0.52.1
python-multipart==0.0.22
aiofiles==25.1.0
anyio==4.12.1

# Numeric / geo
numpy==1.26.4
scipy==1.15.2
pillow==11.2.1
opencv-python==4.11.0.86
shapely==2.1.0
trimesh==4.6.8
rtree>=1.0           # BVH index required by trimesh ray-casting (stl_import.py)
requests==2.32.5

# Optional: OSM city data (osmnx used by cities router)
# osmnx==1.9.4

# Geo raster I/O (used by height providers: WSF3D, Copernicus, 3DEP, nDSM)
rasterio>=1.3

# Optional: satellite imagery
# scikit-image==0.25.2

# Optional: CNN height prediction (Phase 2)
# Uncomment (and optionally add segmentation-models-pytorch) to enable
# predict_heights() and train_height_model() in TerrainSession.
# torch>=2.0
# torchvision>=0.15
# timm>=0.9
# transformers>=4.40
# segmentation-models-pytorch>=0.3  # optional U-Net via smp; timm fallback used otherwise

# Building height — Overture Maps GeoParquet via S3 (Open Buildings provider)
# Install: pip install pyarrow[s3]>=14 fsspec>=2023.1 s3fs>=2023.1
pyarrow[s3]>=14
fsspec>=2023.1
s3fs>=2023.1
