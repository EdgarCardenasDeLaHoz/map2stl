build
dist
*.egg-info
__pycache__
.ipynb_checkpoints
.pytest_cache

# Local generated rasters / images (not source of truth)
cache
*.tif
*.tiff

# Local config — may contain API keys; do not commit
config.json
.env
.env.*
*.gcode
*.curaprofile
*.ini

# Runtime / generated
data.db
data.db-shm
data.db-wal
server.log
osm_raw_cache
ee_cache/*

# Node / Vite
node_modules/
package-lock.json

# Built docs sites
docs_site/
sphinx_site/

# Runtime training logs and metric snapshots (kept locally)
logs/*.log
*_history.json

# Output: generated PDFs, PNGs are large binaries — don't commit
output/*

# Trained model checkpoints — large, regenerable from training scripts
models/*.pt
models/*_inspect.pdf/*.png
# skyline_cv generated output (PDFs, PNG page dumps, image cache) —
# regenerable from city2stl/skyline_cv/scripts/08_region_skyline_pdf.py
city2stl/skyline_cv/runs/
runs/*
