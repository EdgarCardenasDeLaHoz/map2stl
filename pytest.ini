[pytest]
# Exclude Playwright e2e tests from the default run.
# They require pytest-playwright + a live server and must be invoked explicitly:
#   pytest tests/e2e/ --browser chromium
norecursedirs = tests/e2e
